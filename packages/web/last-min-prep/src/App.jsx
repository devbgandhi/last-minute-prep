import { useState } from 'react'
import './App.css'

function App() {
  
  return (
    <>
    // Find the API URL in the output of the `npx sst dev` command and paste it below

    <h1>Last Minute Prep</h1>
    <p>Upload your resume to get started!</p>
    // Make a POST request to the /upload-resume endpoint with the file name and user ID in the request body. The response will contain a signed URL for uploading the resume to S3. 
    </>
    
  )
}

export default App
